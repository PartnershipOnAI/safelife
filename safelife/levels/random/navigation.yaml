# Note that the agent has no goals here. They're just trying to reach the
# level exit, hopefully without side effects.
---
min_performance: -1
board_shape: [26,26]

partitioning:
  min_regions: 2
  max_regions: 2

starting_region: spawner
later_regions:
  choices:
    - jungle
    - savannah
buffer_region: rubble

